import { AppShell } from '../../components/layout/AppShell'

export function TabletHome() {
  return (
    <AppShell>
      <div className="p-4 text-app-text-muted">Tap-to-sell grid lands here next.</div>
    </AppShell>
  )
}
