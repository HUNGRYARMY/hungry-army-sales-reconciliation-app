// Placeholder — regenerate once Docker is available locally:
//   supabase gen types typescript --db-url "<connection-string>" > src/types/database.types.ts
// (gen types shells out to a local postgres-meta container; it isn't runnable in this environment.)
// Do not hand-edit table shapes here once the generated version lands.
export type Database = Record<string, unknown>
